{
  "name": "typescript-factory-unpacked",
  "version": "0.0.1",
  "description": "Wrapper around the Typescript Compiler API that changes the parameters to a single object so you can omit unused paramters making for a cleaner function call.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "generate": "npx tsx src",
    "generate-watch": "npx tsx watch src"
  },
  "author": "Anthony de Jong",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.1",
    "@types/typescript": "^2.0.0",
    "fs": "^0.0.1-security",
    "typescript": "*"
  },
  "peerDependencies": {
    "typescript": "^5.3.2"
  }
}
